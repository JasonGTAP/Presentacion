@model IEnumerable<Conexion.Models.Reporte>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FechaReporte)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estacionamiento.IdEstacionamiento)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FechaReporte)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estacionamiento.IdEstacionamiento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdReporte }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdReporte }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdReporte })
        </td>
    </tr>
}

</table>
